{
  "name": "exercise-01-introductions-web",
  "version": "0.1.0",
  "private": true,
  "description": "Exercise 01 webapp — four live proofs (activations, depth, embeddings, data); hand-written JS, no libraries",
  "scripts": {
    "_comment": "Hosting moved to Vercel (see deploy/README.md). These Netlify scripts + devDeps are retained but deactivated; deploy is now via Vercel Git integration or `npx vercel`.",
    "deploy:netlify": "netlify deploy --dir=web",
    "deploy:netlify:prod": "netlify deploy --prod --dir=web"
  },
  "devDependencies": {
    "@netlify/sdk": "^3",
    "netlify-cli": "^17"
  }
}

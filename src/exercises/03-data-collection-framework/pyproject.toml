[project]
name = "exercise-03-data-collection-framework"
version = "0.1.0"
description = "A framework for deciding the pre-training data mix of an India-first LLM: grade sources, budget tokens, measure fertility"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dataframework"]

[project]
name = "exercise-09-loss-functions-output-heads"
version = "0.1.0"
description = "Exercise 09 — Loss functions and output heads"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2",
    "tokenizers>=0.20",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lossheads"]
